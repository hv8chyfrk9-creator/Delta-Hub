local CoreGui = game:GetService("CoreGui")

-- Limpiar loader anterior si existe
if CoreGui:FindFirstChild("DeltaHubLoader") then
    CoreGui.DeltaHubLoader:Destroy()
end

-- Creación de la Interfaz del Loader
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "DeltaHubLoader"
ScreenGui.Parent = CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.5, -150, 0.5, -100)
MainFrame.Size = UDim2.new(0, 300, 0, 200)
MainFrame.Active = true
MainFrame.Draggable = true

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 8)
UICorner.Parent = MainFrame

-- Barra Superior
local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.Parent = MainFrame
TopBar.BackgroundColor3 = Color3.fromRGB(35, 35, 42)
TopBar.BorderSizePixel = 0
TopBar.Size = UDim2.new(1, 0, 0, 35)
Instance.new("UICorner", TopBar).CornerRadius = UDim.new(0, 8)

local TopBarFix = Instance.new("Frame")
TopBarFix.Parent = TopBar
TopBarFix.BackgroundColor3 = Color3.fromRGB(35, 35, 42)
TopBarFix.BorderSizePixel = 0
TopBarFix.Position = UDim2.new(0, 0, 1, -5)
TopBarFix.Size = UDim2.new(1, 0, 0, 5)

local Title = Instance.new("TextLabel")
Title.Parent = TopBar
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 15, 0, 0)
Title.Size = UDim2.new(0, 200, 1, 0)
Title.Font = Enum.Font.GothamBold
Title.Text = "Delta Hub | Selector de Juegos"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 14
Title.TextXAlignment = Enum.TextXAlignment.Left

-- Botón Cerrar (X)
local CloseBtn = Instance.new("TextButton")
CloseBtn.Parent = TopBar
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
CloseBtn.Position = UDim2.new(1, -30, 0, 6)
CloseBtn.Size = UDim2.new(0, 24, 0, 24)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 12
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 4)

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- Contenedor de Botones
local ButtonsFrame = Instance.new("Frame")
ButtonsFrame.Parent = MainFrame
ButtonsFrame.BackgroundTransparency = 1
ButtonsFrame.Position = UDim2.new(0, 20, 0, 55)
ButtonsFrame.Size = UDim2.new(1, -40, 1, -75)

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Parent = ButtonsFrame
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, 15)
UIListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
UIListLayout.VerticalAlignment = Enum.VerticalAlignment.Center

-- Botón 1: Escapa del Teclado
local BtnTeclado = Instance.new("TextButton")
BtnTeclado.Parent = ButtonsFrame
BtnTeclado.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
BtnTeclado.Size = UDim2.new(1, 0, 0, 40)
BtnTeclado.Font = Enum.Font.GothamBold
BtnTeclado.Text = "Escapa del Teclado"
BtnTeclado.TextColor3 = Color3.fromRGB(255, 255, 255)
BtnTeclado.TextSize = 14
Instance.new("UICorner", BtnTeclado).CornerRadius = UDim.new(0, 6)

-- Botón 2: Escapa del Mono
local BtnMono = Instance.new("TextButton")
BtnMono.Parent = ButtonsFrame
BtnMono.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
BtnMono.Size = UDim2.new(1, 0, 0, 40)
BtnMono.Font = Enum.Font.GothamBold
BtnMono.Text = "Escapa del Mono"
BtnMono.TextColor3 = Color3.fromRGB(255, 255, 255)
BtnMono.TextSize = 14
Instance.new("UICorner", BtnMono).CornerRadius = UDim.new(0, 6)

-- Funciones de Carga
BtnTeclado.MouseButton1Click:Connect(function()
    BtnTeclado.Text = "Cargando..."
    BtnTeclado.BackgroundColor3 = Color3.fromRGB(100, 255, 100)
    BtnTeclado.TextColor3 = Color3.fromRGB(0, 0, 0)
    task.wait(0.5)
    ScreenGui:Destroy()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/hv8chyfrk9-creator/Delta-Hub/refs/heads/main/teclado.lua"))()
end)

BtnMono.MouseButton1Click:Connect(function()
    BtnMono.Text = "Cargando..."
    BtnMono.BackgroundColor3 = Color3.fromRGB(100, 255, 100)
    BtnMono.TextColor3 = Color3.fromRGB(0, 0, 0)
    task.wait(0.5)
    ScreenGui:Destroy()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/hv8chyfrk9-creator/Delta-Hub/refs/heads/main/mono.lua"))()
end)
